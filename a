<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>連絡フォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Hiragino Kaku Gothic ProN","Yu Gothic", Meiryo, sans-serif; padding: 20px; }
    .container { max-width: 640px; margin: 0 auto; }
    label { display:block; margin-top:12px; font-weight:600; }
    select, textarea, input[type="text"] { width:100%; padding:8px; margin-top:6px; box-sizing:border-box; }
    button { margin-top:14px; padding:10px 14px; background:#007bff; color:#fff; border:none; border-radius:6px; cursor:pointer; }
    .note { font-size:0.9em; color:#666; margin-top:8px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>連絡フォーム</h1>
    <p class="note">クラス・出席番号・内容を入力して「送信」してください。</p>

    <!-- ここが重要：action を使うサービスに合わせて変更 -->
    <!-- 例1: Formspree を使う場合: action="https://formspree.io/f/あなたのID" method="POST" -->
    <!-- 例2: Formsubmit.co を使う場合: action="https://formsubmit.co/あなたのメールアドレス" method="POST" -->
    <form id="contactForm" action="https://formspree.io/f/REPLACE_WITH_YOUR_FORM_ID" method="POST" novalidate>
      <label for="class">クラス</label>
      <select id="class" name="class" required>
        <option value="3-1">3-1</option>
        <option value="3-2">3-2</option>
        <option value="other">その他</option>
      </select>

      <div id="otherClassWrap" style="display:none;">
        <label for="otherClass">その他（クラス名を入力してください）</label>
        <input type="text" id="otherClass" name="other_class" placeholder="例：2-A">
      </div>

      <label for="seat">出席番号</label>
      <select id="seat" name="seat_number" required>
        <!-- 1〜40 を自動で作る -->
      </select>

      <label for="content">内容</label>
      <textarea id="content" name="content" rows="6" required placeholder="ここにメッセージを書いてください"></textarea>

      <!-- Honeypot（スパム防止用、ユーザーに見えない） -->
      <div style="display:none;">
        <label>Leave this field empty</label>
        <input type="text" name="_honey">
      </div>

      <!-- Formspree や Formsubmit に追加パラメータ（リダイレクト先等）を渡すこともできます -->
      <!-- <input type="hidden" name="_next" value="https://your-site/thanks.html"> -->

      <button type="submit">送信</button>
    </form>

    <p class="note">送信先の設定については下の「送信先の設定」を参照してください。</p>
  </div>

  <script>
    // 出席番号の選択肢を 1〜40 で生成
    (function(){
      var seat = document.getElementById('seat');
      for (var i=1;i<=40;i++){
        var opt = document.createElement('option');
        opt.value = i;
        opt.text = i;
        seat.appendChild(opt);
      }
    })();

    // 「その他」を選択したら入力欄を表示
    document.getElementById('class').addEventListener('change', function(e){
      var wrap = document.getElementById('otherClassWrap');
      if (e.target.value === 'other') wrap.style.display = 'block';
      else wrap.style.display = 'none';
    });

    // 簡易バリデーション（HTML5 required で十分だが補助）
    document.getElementById('contactForm').addEventListener('submit', function(e){
      var cls = document.getElementById('class').value;
      var other = document.getElementById('otherClass').value.trim();
      if (cls === 'other' && other === ''){
        e.preventDefault();
        alert('「その他」を選んだ場合はクラス名を入力してください。');
        return false;
      }
      // フォームは action 設定に従って送信される
    });
  </script>
</body>
</html>
